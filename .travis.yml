language: generic 
sudo: required 
services:
  - docker

before_install:
  - docker build -t devonnadang/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true devonnadang/docker-react npm run test